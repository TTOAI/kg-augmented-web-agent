# Cascade configuration for GitLab (WebArena-Verified instance).
#
# Phase 3.H Tier 2: 이전에는 site_adaptive_webagent/kg_solution/path_finder.py 에
# DEFAULT_GITLAB_CONFIG 이름으로 하드코드되어 있던 값. path_finder의 fallback 6-stage
# cascade (exact → family_sibling → scope_entry → hub_fallback → stay_and_explore → failed)
# 중 scope_entry / hub_fallback 단계에서 참조.
#
# 다른 site 적용 시 동일 schema로 scope name → entry class path 매핑 작성.

# 각 scope의 "entry point" class. cascade 시 target class의 scope에 해당하는 entry로
# 우회 가능한지 확인.
scope_entries:
  project: project/main
  dashboard: dashboard/project_list/yours
  account: account/account
  global: global/root_redirect
  user: user/profile
  explore: explore/project_list/all
  ide: ide/edit_view

# 모든 scope가 실패했을 때 최후 fallback hub. 일반적으로 site의 landing / 사용자 홈.
hub: dashboard/project_list/yours

# Class 이름의 마지막 segment가 이 집합에 속하면 variant suffix로 인식 (예:
# dashboard/project_list/yours → base는 dashboard/project_list, variant는 yours).
# extract_family() / _find_family_siblings에서 family key 추출 시 사용.
# Phase 3.H Tier 3b: 이전엔 path_finder.py 모듈 상수 VARIANT_SEGMENTS로 하드코드.
variant_segments:
  - yours
  - starred
  - all
  - trending
  - pending
  - done

# Class name 수준의 family 추출에 사용되는 type suffix 목록. 길이 긴 것 먼저.
# 예: project/issue_new_form → suffix `_new_form` 제거 → family `project/issue`.
# Phase 3.H Tier 3b: 이전엔 path_finder.py 모듈 상수 FAMILY_TYPE_SUFFIXES로 하드코드.
family_type_suffixes:
  - _new_form
  - _edit_form
  - _pipelines
  - _commits
  - _diff
  - _feed
  - _board
  - _list
  - _detail
  - _view
  - _form
  - _graph
  - _search
