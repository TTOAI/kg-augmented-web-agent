# L3: task 308 — KG 무관한 평가기 strict-match 결함 (RET)

**Type**: RETRIEVE
**Source**: WebArena-Verified task 308

## Intent
> Get the username(s) of the user(s) with the most commits to the primer/design project

Start URL: `http://localhost:8023`

## Hypothesis
Task 308은 H1 (309)과 동일 archetype (contributor graph URL)이지만, evaluator가 답 포맷에 strict-match를 요구해 의미적으로 정답인 답이 **답 포맷 불일치**로 fail로 기록된다. V0와 V1 모두 페이지에 도달하더라도 평가기는 fail. 본 cell은 "KG가 도달까지 작동해도 평가기 인공 요소로 outcome이 KG 효과와 분리됨"을 transparent하게 보여주기 위한 condition이다.

## Predicted V0
- step 1~5: H1 (309) 동일 패턴으로 contributor graph 페이지 도달, 상위 사용자명 추출, report_success(answer=...).
- 평가기: agent evaluator strict-match로 fail 가능 (이메일 vs 실명, 추가 공백 등).
- 예상 step 수: 5~7, outcome FAIL (evaluator artifact)

## Predicted V1
- step 1~2: KG 힌트로 contributor graph 단축 도달, answer 추출.
- 평가기: V0와 동일 사유로 strict-match fail.
- 예상 step 수: 2~3, outcome FAIL (evaluator artifact)

V1이 step은 줄지만 outcome은 둘 다 fail이라는 점이 핵심. KG 효과와 평가 인공 요소가 분리됨을 보여줌.

## Mechanism invocation signal (V1 log)
- H1과 동일: `[KG] inferred target=project/contributor_graph`, `/-/graphs/` URL 도달.
- post-measurement 수동 검증: V1의 `agent_response.json.retrieved_data`가 의미적으로 정답이지만 evaluator가 reject — `eval_exclusions.md`에 broken evaluator로 분류.

## Falsification criterion
- V0 또는 V1이 strict-match에 통과 (즉 평가기 artifact가 발현되지 않음 → 본 cell이 L3가 아님)
- V1의 retrieved_data가 V0와 본질적으로 다름 (KG가 답 자체를 변형 → 다른 메커니즘이 작동)

→ 둘 중 하나라도 성립하면 refuted.

## Notes
- prior OpenAI 측정에서 V0/V1 모두 응답 평가기 fail (답 포맷 불일치) 확인. Claude에서 동일 패턴인지가 핵심 — Claude는 답 포맷에 더 엄격할 가능성도 있어 결과가 다르게 나올 수 있음.
- 만약 Claude가 정답 포맷을 운 좋게 맞히면 본 cell은 inconclusive로 분류. 본 task picks의 가장 fragile한 cell.
- L3는 KG 무관 결함을 transparent하게 노출하는 게 목표 — pass/fail 자체보다 "왜 fail인가"의 narrative가 paper의 가치.
